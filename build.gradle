apply plugin: 'java'
apply plugin: 'idea'

dependencies {
  compile group: 'org.codehaus.groovy', name: 'groovy-all', version: '2.1.3'
}

idea {
  project {
    jdkName = '1.7'
    languageLevel = '1.7'
  }
}